server:
  port: 8080

spring:
  application:
    name: gateway-servicio
  cloud:
    config:
      enabled: false
  config:
    import-check:
      enabled: false

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
